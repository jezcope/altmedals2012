%p.description= @description
- if @last_updated.nil?
  %p.last-updated
    No data?
    %a{href: url('/update')} Let's go get some!
- else
  %p.last-updated
    Data last updated #{@last_updated.strftime('at %R on %A %-d %B %Y')}.
    Not fresh enough?
    %a{href: url('/update')} Update it!
%table#medals
  %thead
    %tr
      %th.pos-header &nbsp;
      %th Name
      %th Gold
      %th Silver
      %th Bronze
      %th Total
  %tbody
    - @nations.reverse_each.with_index do |nation, i|
      %tr{id: nation.code}
        %td.position=     i + 1
        %td.country_name= nation.name
        %td.gold=         nation.gold
        %td.silver=       nation.silver
        %td.bronze=       nation.bronze
        %td.total=        nation.total

